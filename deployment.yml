apiVersion: apps/v1
kind: Deployment
metadata:
  name: multiplayertest
  namespace: tenant-7654b5-asrpods
  labels:
    app: multiplayertest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: multiplayertest
  template:
    metadata:
      labels:
        app: multiplayertest
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node.coreweave.cloud/cpu
                    operator: In
                    values:
                      - intel-xeon-v3
      containers:
        - name: multiplayertest
          image: exartadev/zeniva.exarta:multiplayertest-v30-asrpods
          ports:
            - containerPort: 5173
          resources:
            requests:
              cpu: 1
              memory: 4Gi
            limits:
              cpu: 1
              memory: 4Gi
          imagePullPolicy: Always
      imagePullSecrets:
        - name: dockerhub-secret
      restartPolicy: Always
