apiVersion: apps/v1
kind: Deployment
metadata:
  name: multiplayer-events
  namespace: tenant-7654b5-asrpods
  labels:
    app: multiplayer-events
spec:
  replicas: 1
  selector:
    matchLabels:
      app: multiplayer-events
  template:
    metadata:
      labels:
        app: multiplayer-events
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node.coreweave.cloud/cpu
                    operator: In
                    values:
                      - intel-xeon-v3
      containers:
        - name: multiplayer-events
          image: exartadev/zeniva.exarta:multiplayer-events-v221-asrpods
          ports:
            - containerPort: 5173
          resources:
            requests:
              cpu: 1
              memory: 4Gi
            limits:
              cpu: 1
              memory: 4Gi
          imagePullPolicy: Always
      imagePullSecrets:
        - name: dockerhub-secret
      restartPolicy: Always
